# commit-msg 钩子
# ---------------------------------------------------------------
# 触发时机：执行 git commit，在你写完提交信息之后、生成提交之前自动运行。
# 作用：校验「提交信息」的格式是否符合规范（规则见根目录 commitlint.config.js）。
#      如果格式不对（比如写成「改了点东西」），本次 commit 会被中断。
#
# 关于 $1：git 会把「本次提交信息的临时文件路径」作为第一个参数传进来，
#         这个文件里存的就是你写的提交信息。commitlint 用 --edit 读取它来校验。
# ---------------------------------------------------------------

# 调用 commitlint 校验提交信息；--edit $1 表示读取 git 传入的提交信息文件
pnpm exec commitlint --edit $1
