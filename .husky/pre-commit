# pre-commit 钩子
# ---------------------------------------------------------------
# 触发时机：执行 git commit，在「真正生成这次提交之前」自动运行本脚本。
# 作用：在提交前对暂存的代码做一次检查（这里是跑 ESLint）。
#      只要下面的命令返回失败（非 0 退出码），本次 commit 就会被中断。
#
# 说明：husky v9 会自动帮我们加载环境，所以这里不需要写多余的头部代码，
#      直接写要执行的命令即可。
# ---------------------------------------------------------------

# 调用 lint-staged：只检查本次 git add 过的文件，规则见根目录 lint-staged.config.js
pnpm exec lint-staged
